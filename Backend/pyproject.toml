[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    "venv",
    ".venv",
    "__pycache__",
    "Uploads",
    "uploads",
    "reportes",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]